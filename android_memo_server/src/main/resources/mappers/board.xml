<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.vue.dao.MemoDAO">

	<!-- 게시글 전체 조회 -->
	<select id="selectMemo" resultType="Memo">
		select no, title, writer, content, regtime
		  from vue_Memo 
		 order by no desc
	</select>
	
	<!-- 게시글 상세 조회 -->
	<select id="selectMemoByNo" resultType="Memo">
		select no, title, writer, content, regtime
		  from vue_Memo 
		 where no = #{no}
	</select>

	<!-- 게시글 등록 -->
	<insert id="insertMemo" parameterType="Memo">
		insert into vue_Memo(
			title, writer, content
		) values (
			#{title}, #{writer}, #{content}
		)
	</insert>
	
	<!-- 게시글 수정 -->
	<update id="updateMemo" parameterType="Memo">
		update vue_Memo 
		   set title = #{title}, 
		   	   writer = #{writer}, 
		   	   content = #{content}
		 where no = #{no}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deleteMemo" parameterType="int">
		delete 
		  from vue_Memo 
		 where no = #{no}
	</delete>

	<!-- 게시글 제목 검색 -->
	<select id="searchMemo" resultType="Memo">
		select no, title, writer, content, regtime
		  from vue_Memo 
		 where title like concat('%', #{keyword}, '%')
		 order by no desc
	</select>
</mapper>

